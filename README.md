# scripts-doctor Lint and auto-fix your **package.json scripts** to be **cross-platform** and reliable. - Rewrites `rm -rf` → `rimraf` (portable delete) - Rewrites `cp|mv|mkdir -p|grep|sed|cat` → `shx <cmd>` (Unix-like on Windows) - Wraps `FOO=bar cmd` → `cross-env FOO=bar cmd` (Windows-safe env vars) - Warns if common CLIs (`tsc`, `jest`, `eslint`, …) are used without being installed locally Why these fixes? - `cross-env` lets you set env vars in scripts consistently across OSes. :contentReference[oaicite:3]{index=3} - `shx` brings Unix commands to Windows via ShellJS; `rimraf` is the cross-platform `rm -rf`. :contentReference[oaicite:4]{index=4} - General guidance on cross-platform scripts: 2ality’s write-up. :contentReference[oaicite:5]{index=5} ## Install ```sh npm i -D scripts-doctor # or npx scripts-doctor ...
